#ifndef DIST_H_
#define DIST_H_

#include <math.h>

float euclid(float *p1, float *p2, int tam);

float m_dist(float *p1, float *p2, int tam, float R);

float c_dist(float *p1, float *p2, int tam);

#endif //DIST_H_